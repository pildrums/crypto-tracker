import styled from 'styled-components';

interface IPrice {}

const Price = () => {
  return (
    <PriceContainer>
      
    </PriceContainer>
  );
};

const PriceContainer = styled.div``;

export default Price;